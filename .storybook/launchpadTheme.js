import { create } from '@storybook/theming/create';

export default create({
  base: 'light',
  colorSecondary: '#ff9700',
  brandTitle: 'Launchpad',
  brandUrl: 'localhost:3000',
  brandImage: '...',
});
